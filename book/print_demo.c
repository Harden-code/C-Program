#include <stdio.h>

int main(int argc, char const *argv[])
{   
    //获取控制台输入字符
    int c=getchar();
    while (c!=EOF)
    {
        //打印到控制台
        putchar(c);
        c=getchar();
        /* code */
    }
    
    /* code */
    return 0;
}
